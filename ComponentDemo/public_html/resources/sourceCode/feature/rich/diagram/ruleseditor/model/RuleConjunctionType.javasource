package oracle.adfdemo.view.feature.rich.diagram.ruleseditor.model;


/**
 * Defines the conjunction type of a group and hence the 
 * links implied for nodes within that group. 
 * Links (unlike Groups) may have a value of null, indicating that that 
 * is an artifical link used to associate a DropTarget with anther node
 * rather than a Link that is part of the ruleSet
 */
public enum RuleConjunctionType {
    AND, OR
}
